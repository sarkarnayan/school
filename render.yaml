services:
  - type: web
    name: school-swagger-api
    runtime: python
    plan: free
    envVars:
      - key: API_KEY
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
